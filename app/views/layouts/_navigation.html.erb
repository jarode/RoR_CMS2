

<a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                        <i class="icon-bar"></i>
                        <i class="icon-bar"></i>
                        <i class="icon-bar"></i>
                    </a>
					<%= link_to "Planato", root_path, :class => 'brand' %>
                    

                    <div class="pull-right" id="user-button">
                        
							 

                            <div id="login-popover-container" data-original-title="">
                                <div class="btn-group">
									 <% if user_signed_in? %>
										<%= link_to 'Wyloguj', destroy_user_session_path, :method=>'delete', :class => "btn btn-success" %>
									  <% else %>
										<%= link_to 'Zaloguj', new_user_session_path, :class => "btn btn-success"%>  
										<%= link_to 'Rejestracja', new_user_registration_path, :class => "btn btn-success" %>
									  <% end %>
                                </div>
                            </div>
                        
                    </div>

                    <div class="nav-collapse">
                        <ul class="nav">
						
                                <li>
								<%= link_to "Aukcje", auctions_path %>
								</li> 
							  
							  
							  <li class="dropdown">
                                <a class="dropdown-toggle" data-toggle="dropdown" >
                                    Moje konto
                                    <i class="caret"></i>
                                </a>
                                <ul class="dropdown-menu">
								
									  <% if user_signed_in? %>
										<li>
										<%= link_to 'Wyloguj', destroy_user_session_path, :method=>'delete' %>        
										</li>
									  <% else %>
										<li>
										<%= link_to 'Zaloguj', new_user_session_path %>  
										</li>
									  <% end %>
									  <% if user_signed_in? %>
										<li>
										<%= link_to 'Edytuj Konto', edit_user_registration_path %>
										</li>
										<% if current_user.has_role? :admin %>
										  <li>
										  <%= link_to 'Admin', users_path %>
										  </li>
										<% end %>
									  <% else %>
										<li>
										<%= link_to 'Rejestracja', new_user_registration_path %>
										</li>
									  <% end %>
                                    
                                </ul>
                            </li>
                        </ul>

                        <form action="h" class="navbar-search pull-right" method="post">
                            <div style="display:none"><input type="hidden" name="" value=""></div>
                            <input class="search-query" name="query" placeholder="Search" type="text" value="">
                        </form>
                    </div>